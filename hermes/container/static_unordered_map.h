#pragma once

#include <iterator>
#include <type_traits>
#include <utility>

namespace hermes::container {

class

    template <typename K, typename V, size_t MAX_SIZE>
    class StaticUnorderedMap {
  struct Node {
    K key;
    V val;
    Node *next;
    Node *prev;
  };
  prev

      private:
};

};  // namespace hermes::container
